### read:prompt-gap

ANSWER: The system prompt is assembled in `src/lucy_api/prompt/sections.py` (the `BUILTIN` tuple, rendered by `render_all`, reading authored markdown from `src/lucy_api/prompt/defaults/*.md`), driven through `src/lucy_api/context/assembler.py:88` and split onto the provider's system channel by `_model_prompt` in `src/lucy_api/turn/prompt.py:237-260`, which keeps only `Band.system` sections and returns `"\n\n".join(system_parts)`. I reconstructed it from the nine system-band files and it is byte-identical to the captured 22,232-character system message (identity, behaviour, tools, safety, lessons, helpers, workspace, memory, context — in that order, nothing else appended). Nothing in it tells the model how to finish a turn: there is no sentence anywhere saying that omitting the plan / writing prose ends the turn, or that once it has the answer it should stop emitting `{"steps": ...}`. The closest text is `defaults/tools.md:75-77` ("finish your answer and say what is still running"), which is scoped to background handles, and `defaults/behaviour.md:1-2` / `:46-53`, which govern the content of an answer but never say how to emit one instead of a plan. The only string that ever reaches the system channel resembling a stop instruction is a runtime notice, not authored prompt — `warning_for` in `src/lucy_api/turn/stop.py:141-146` ("N of M model rounds left; finish or write down where you got to"), appended at `turn/prompt.py:212` and only once ≥80% of the iteration budget is spent. The right home for a new instruction is `src/lucy_api/prompt/defaults/tools.md`, which renders as the `## Using tools` section and is one of the two sections no setting can override or disable (`sections.py:306-316`); insert it as a new `###` subsection immediately after the opening "Plan the whole thing" block (after line 9) rather than at the end, because `_shrink_lines` trims prose from the tail and that section already sits at ~961 of its 1100-token ceiling.

UNCERTAIN: Three things I could not settle by reading this repo. (1) The plan schema's own wording is produced by the third-party `weftai` library via `registry.plan_schema({\"maxSteps\": steps})` (src/lucy_api/packs/registry.py:298-309), so I verified its shape only from the captured request body, not from its generating source; I cannot rule out a terminating hint inside weftai's own per-op descriptions beyond the 39 op consts and the one `steps` description I read ("Steps to run. A step may reference earlier steps' results with $id; independent steps run together."). (2) I checked only the first captured request (sent-01.json); later rounds could in principle carry the `warning_for` notice or a repair notice in the system channel, which I did not observe directly. (3) I did not verify whether `src/lucy_api/prompt/defaults/` is ever shadowed at runtime by a settings override for the behaviour/identity sections in this particular deployment -- the two candidate sections for a terminating rule (

### read:intended-design

ANSWER: The suite's finishing reply is unambiguous: a `Reply` with `plan=None` and non-empty `text`, produced by `speaks(...)` (scripted.py:81-90, which leaves `Reply.plan` at its None default, types.py:115) and consumed by loop.py:240-241 (`if reply.plan is None or executor is None: return _finished(...)`). Concretely, `test_a_plan_is_run_and_the_model_gets_another_go` (test_turn_loop.py:144) scripts `[plans(PLAN), speaks("Three dates in March.")]` — plan round runs the tools, prose round ends the turn — and this is structural, not incidental: every multi-round test ends its script with a `speaks(...)` step, and a script that runs out instead raises ScriptExhaustedError and fails the turn (scripted.py:200-212; test_turn_loop.py:501). So yes, the design assumes the model freely chooses plan-or-prose each round: Reply's docstring says plan "is None when the model simply spoke, which is the common case" (types.py:109-111), the loop's pseudocode says "if no plan: finish" (loop.py:8-12), and reply_from tolerates prose even when a plan was wanted (chat.py:155-161 with wire.py:91-101). That assumption is NOT compatible with what `_body` sends: whenever `request.plan_schema is not None` it attaches response_format json_schema (chat.py:269-274), and plan_schema is never None in production (supervisor.py:314 always passes a callable, packs/service.py:193-196 returns dict[str, Any], loop.py:206 calls it every round). The captured schema has required ["steps"], steps.minItems 1 and additionalProperties false, so under constrained decoding the only legal output is an object with at least one step — the prose reply the whole design uses to end a turn is unrepresentable on the wire. The suite cannot see this because ScriptedProvider.complete serves a pre-built Reply and never looks at request.plan_schema (scripted.py:187-189), test_turn_loop.py never sets plan_schema at all (helper at test_turn_loop.py:102), and no test drives run_turn/TurnSupervisor through a real ChatProvider.

UNCERTAIN: I did not read weftai's schema generator, so I cannot cite source for where minItems:1 originates (packs/registry.py:298-309 delegates to registry.plan_schema); I took required/minItems from the captured request body as instructed. I also did not empirically verify that the clyde harness enforces response_format - the incompatibility argument assumes the provider honors it, which is what spec.traits.json_schema=True asserts. If clyde ignored it, the same non-termination would instead point at the system prompt, which in the captured 22,232-char message contains zero occurrences of "JSON" and never tells the model that answering in prose is how a turn ends. I also did not verify the Anthropic/OpenAI adapters beyond noting they attach the same schema unconditionally (anthropic.py:288-289, openai.py:268-273).

### read:loop-exit

ANSWER: A Lucy turn has exactly seven exit points in `run_turn`, and only one is the intended normal end: `_finished` (loop.py:308-314), reached from loop.py:240-241 when `reply.plan is None or executor is None` — the model spoke prose instead of JSON, giving `Termination.success` / `Stop.end_turn`, and the person sees the text `_assistant_item` appended at loop.py:322-324. The other six are: (1) pre-round cancel in `_early_stop` (loop.py:193-197 → 619-626, `cancelled`); (2) pre-round budget stop (loop.py:627-629 → `_stopped` 633-636) taking one of four `should_stop` verdicts (stop.py:107-130) — seconds and tokens give `error_max_budget`, tool calls and rounds give `error_max_iterations`; (3) `_ask` returning None (loop.py:214-216) for a refusal (`error_refused`, loop.py:405-409) or a failure (`error_during_execution`: unavailable 410-414, unknown exception 415-418, empty stream 419-422); (4) the same cancel/budget check re-run after a reply (loop.py:235-238), which appends the round first; (5) `_parked` (loop.py:254-255, 450-470) → `input_required` with the approval asks; (6) repair exhaustion after `MAX_PLAN_REPAIRS`=2 (loop.py:293-296) → `failed`, "the plan could not be repaired". `Termination.auth_required` is in the enum but no loop path produces it, and an exception from `assemble`/`execute`/`append` (loop.py:251 is unguarded) escapes `run_turn` entirely, leaving the turn row stuck in `running`. Critically, there is no repetition exit at all: `Repetition` is constructed at loop.py:189 and fed at loop.py:545, but `notice_for`, `should_drop`, `is_dropped`, `drop` and `summary` have no call sites anywhere in `src`, so the repetition machinery neither warns the model nor ends a turn. What actually ended the captured real run is the success path in disguise: the model fenced its plan in a ```json block, `json_object` is a bare `json.loads` (wire.py:91-101) so it returned None, `reply_from` kept the raw text (chat.py:160), and `_finished` recorded success/end_turn while the person was shown the raw plan JSON as the assistant's answer — a bad outcome, because the worst failure mode (the model never answering) is recorded identically to a real answer and the person is handed wire format as prose.

UNCERTAIN: I could not reproduce the specific run described as ending on "a repetition check (recorded as success)" - no such check exists in run_turn, should_stop, the weftai package in .venv, or anywhere else in src - so I cannot say what that observation referred to; the only captured artefact (2 requests plus last-turn.json) ends via _finished on a fenced-JSON reply, which produces exactly the reported symptom (success plus raw plan JSON shown as the answer). I did not trace the 8-round and 12-round attempts, whose transcripts are not in the capture directory, so I cannot rule out that one of them ended on error_max_iterations (stop.py:125-130) rather than on a fenced reply. I also did not verify the API/CLI rendering layer end to 

### refute:prompt-gap

HOLDS=True | strongest: C:/Users/tochi/Documents/Code/LUCY-assistant/src/lucy_api/turn/prompt.py:238

CORRECTION: Every load-bearing citation is real and means what is claimed; I re-derived the byte-identical 22,232-char reconstruction and re-measured tools.md at 961/1100 with the real runtime counter. Four non-fatal corrections. (1) The "reading authored markdown from defaults/*.md ... nothing else appended" claim is loose for one of the nine: the behaviour section uses render=_behaviour (sections.py:300, defined 178-189), which appends an extra sentence when response_style is "brief" or "thorough"; the reconstruction is byte-identical only because the observed run used the default "natural" (turn/prompt.py:44). Neither extra sentence is a terminating instruction. Similarly, render_all takes overrides/disabled (sections.py:513-546) that can replace or drop seven of the nine system sections — not tools/safety, and none were in play. (2) "The closest text is tools.md:75-77" understates: defaults/helpers.md:47 ("Do not wait in a loop. Carry on, or finish and say what is still running.") is equally close, and context.md:4 says "finish what is already started". Both are scoped the same way and neither states the mechanism, so this strengthens the finding. (3) The runtime `notice` at loop.py:199-201 is warning_for joined with cycle.repair_notice (set at loop.py:258 and 263-268); the investigator cited only warning_for. Repair notices are invalid-plan/denial text, not stop instructions. (4) Missed entirely: turn/repetition.py:80-82 injects "Either use that result, change the arguments, or say what is blocking you" after 2 identical calls — the only runtime string that pushes toward prose. It arrives as a tool-result data message, not on the system channel, and still never says that a reply without a plan is what ends the turn, so the gap stands. Also verified: exactly 39 dotted step ops in the captured schema (the extra bare `op` values eq/gt/contains are note-filter predicates), required:["steps"], minItems 1 (and maxItems 20), none named finish/respond/answer/done; Band.system occurs at exactly 9 places in sections.py and nowhere else in src/lucy_api; the capture's three non-system messages (capabilities, live state, question) contain no terminating instruction either.

### refute:intended-design

HOLDS=True | strongest: C:/Users/tochi/Documents/Code/LUCY-assistant/src/lucy_api/turn/loop.py:240

CORRECTION: The structural claim survives: every production round attaches a plan_schema (supervisor.py:314 and agents/runtime.py:375 are the only run_turn call sites besides loop.py:181; packs/service.py:193-196 -> registry.py:298-310 always returns a dict, never None), chat.py:269-274 therefore always sets response_format, and the loop's only successful exit is a reply with plan=None (loop.py:240). Every quoted line is real and says what was claimed. Three corrections, none fatal:

1. OVERSTATED MECHANISM. "Constrained decoding" / "unrepresentable on the wire" is stronger than the code. chat.py:271-274 sends json_schema with only `name` and `schema` and NO `strict: true`, and that is deliberate and documented: openai.py:39-41 "It does not ask for strict schema enforcement: a compiled weftai plan schema uses constructs strict mode refuses" (same note at anthropic.py:38-40). Without strict, a compatible server is not grammar-locked, so prose can come back, reply_from yields plan=None (chat.py:155), and the turn would end normally. The fault is better stated as: the wire never offers a prose option (the else branch at chat.py:275-277 falls back to {"type":"json_object"} plus the schema in words, also JSON-only), so ending a turn depends on the provider disobeying the response_format it was sent.

2. THE OBSERVED RUN HAD NO GRAMMAR CONSTRAINT AT ALL. clyde does not do constrained decoding: C:/Users/tochi/Documents/Code/clyde/src/clyde/openai/translate.py:70-82 lifts the schema out, and C:/Users/tochi/Documents/Code/clyde/src/clyde/cli/argv.py:131-144 either passes it as `--json-schema` to the Claude Code CLI (argv.py:180) or, when argv still will not fit, appends it to the prompt as words and drops the flag (argv.py:136-138: "a schema in words is a request where a schema on the command line is a guarantee"). So the 8/12 identical plans are a model obeying an instruction, not a decoder that could emit nothing else. The conclusion (no reachable prose exit) is unchanged; the causal story is.

3. LOOSE LINE ANCHORS (not fabrications): test_turn_loop.py:501 is the test's def line, the quoted ScriptedProvider([plans(PLAN)]) is line 509; test_turn_live_settings.py:105 is the def line, the scripted provider is 116 and the plan_schema assertion is 127; test_turn_loop.py:102 is `def turn`, the defaults dict is 103; chat.py:153 spans 153-155.

Additional support the investigator missed: the captured schema's top level is exactly {steps} with additionalProperties false and none of its 39 op constants is a say/answer/reply op, so a plan cannot deliver prose either; the captured body matches chat.py._body (max_tokens/messages/stream_options), confirming ChatProvider is the live module rather than openai.py; and Repetition.notice_for/seen/dropped have no consumer anywhere in src (only repetition.record at loop.py:545), so loop.py:28's "i

### refute:terminal-op

HOLDS=True | strongest: C:/Users/tochi/Documents/Code/LUCY-assistant/src/lucy_api/turn/loop.py:305

CORRECTION: The central answer holds — every load-bearing citation is real and at the stated line, and there is no terminal op among the 39 (I read all 39 descriptions from the capture; agents.message steers a helper, not the person). Four corrections. (1) "The only way to end a turn is to emit text that fails JSON parsing" is false as written: cancellation and the time/token/tool-call/iteration caps (loop.py:193-197, 234-237 -> _early_stop at loop.py:613-630, stop.py:96-131), a permission park (loop.py:253-255 -> _parked at loop.py:450-471, input_required), an unrepairable plan (loop.py:293-296, failed), and a provider failure/refusal (loop.py:215-216) all return an Outcome. The defensible form is narrower: _finished (loop.py:306-312) is the only path that can return Termination.success, and it is gated on reply.plan is None. (2) "An act the response_format is explicitly configured to prevent" is untrue of this capture: .response_format.json_schema.strict is null in both sent-01.json and sent-02.json, and got-02.txt shows the model returning a markdown-fenced ```json block — output no constrained decoder emits. clyde is not enforcing the schema. Since json_object (model/wire.py:97-101) is a bare json.loads with no fence stripping, that fenced reply yields plan=None -> _finished -> Termination.success with the fenced plan JSON shown to the person. That, not a repetition check, explains the observed "success with raw plan JSON as the answer"; turn/repetition.py never ends a turn (it only notices at NOTICE_AT=2 and drops at DROP_AT=4, repetition.py:28-34,85-93). Prose is not prevented, merely never chosen. (3) "Neither is an op doing it" about _parked is imprecise: _permission_issues (loop.py:477-485) fires on permission_required, produced by whichever write-effect op the model picked — an op does end the turn there, it just does not answer. (4) The system-prompt evidence is garbled: it claims grep -niE 'json|schema' matched 5 lines; actual counts on the 22,232-char system message are json 0, schema 0, plan 5 — the grep matches nothing, and the 5 is the plan count (conclusion right, stronger than stated). Also registry.py:201 is cited as a file:line for shell output (it is the build_registry docstring), and the op universe is not closed by grepping src/: packs/mcp.py:119 registers ops under names an MCP server supplies — harmless, since any such op still hits loop.py:305. Additional finding the investigator missed, which strengthens the case: the system prompt lines 96-110 demand "Each step carries one sentence... saying what that call is for", but every step variant is exactly {id, op, input, show_from} with additionalProperties:false and no input exposes a note field, so a model obeying that section emits a schema-invalid plan.

### refute:loop-exit

HOLDS=True | strongest: C:/Users/tochi/Documents/Code/LUCY-assistant/src/lucy_api/turn/loop.py:240

CORRECTION: The conclusion survives: every load-bearing citation is real and means what is claimed. I re-read loop.py in full, stop.py, wire.py, chat.py, repetition.py, project.py, supervisor.py, sql_store.finish_turn, state.py/sources.py, context.md and the capture files, and ran exhaustive greps. Verified: the seven exits; `_finished` as the only producer of Termination.success (grep of every `Termination.*` assignment in src shows assignments only at loop.py:294, 312, 369, 406, 411, 416, 420, 459, 623, plus the default at 106); `should_stop` has exactly one call site (loop.py:627); `Termination.auth_required` is never assigned in the loop; the repetition machinery has only `record` called (loop.py:545) and `notice_for`/`should_drop`/`is_dropped`/`drop`/`summary`/`seen` have zero call sites in src; `json_object` is a bare json.loads (wire.py:91-101) and nothing anywhere in model/ or turn/ strips ``` fences; `_run` guards only `self._models.resolve` with an except (supervisor.py:174-179) so an exception from assemble/execute/append escapes run_turn and `_drain` (try/finally only, supervisor.py:161-171, 303-346), leaving the row `running` until interrupt_abandoned_turns (supervisor.py:127). The captured run is genuinely one turn: sent-02.json is the same session (ses_CAGddxt1hnbcq...) and carries round 1's capabilities.list tool result, and got-02.txt fences the plan -- so the "success in disguise" path is real. THREE CORRECTIONS. (1) "stop_reason end_turn can only come from _finished" is FALSE: Outcome.stop_reason defaults to Stop.end_turn (loop.py:108) and is assigned at only three sites (310, 408, 625), so budget stops, repair exhaustion and parked turns are all recorded with end_turn too -- model/types.py:51-53 says exactly this ("A turn can end in error_max_iterations while every model call in it ended in end_turn"). Only the PAIR termination=success + end_turn is diagnostic. (2) last-turn.json is a DIFFERENT run from the captures: its session is ses_oabn-KOOuTnnkQ... with created_at 1790187873, while the captured requests are session ses_CAGddxt1hnbcq... at 1790186607/1790186624 -- 21 minutes earlier. It does not "corroborate the path taken" by got-02.txt; it is a separate ~102s turn that independently ended success/end_turn. Its iterations/input_tokens/output_tokens of 0 are also meaningless: finish_turn (sql_store.py:768-812) writes only status/termination/stop_reason/finished_at and the word "iterations" appears nowhere in sql_store.py. (3) Two line numbers drift: `text="" if plan is not None else text` is chat.py:163 (160 is the `else:`), and the stream-suppression condition is project.py:68 (not 66). Minor additional point they missed, which supports them: _status_for (supervisor.py:561-562) also maps Termination.refused to "completed".